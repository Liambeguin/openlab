#ifndef TYPES_H
#define TYPES_H

/* define here all the structures that contain the data relative to the 
 * drone.
 * Usage : 
 * 	in main(void)... you should declare the drone struct that contains 
 *	the other sub structs 
 *
 *	then each module gets a pointer to the sub structs he needs
 */
/*struct RC data
struct IMU data
struct GPS data 
struct info

struct drone {
	info,
	IMU data,
	GPS data,
	RC  data,
	...
}
*/
#endif /* TYPES_H */
